Primeiro módulo.

"gets" é implementada dentro do sistema para ler as entradas (inputs) dos dados
"print" imprime a saída (output) de dados e já pula uma linha ("\n")

Comandos terminal (CMD)

-cd (mudar)
-dir (listar)
-mkdir (criar)
-del / rmdir (deletar)

Git/GitHub

cd (para buscar o arquivo)
git add . (adicionar todos os arquivos que estão na pasta)
git status (verificar se todos os arquivos estão adicionados)
git commit -m ""(commitar os arquivos sendo(-m) para por uma mensagem)
git push origin main (passar do servidor local para o remoto)
git pull (passar do servidor remoto para o local)
git clone (clonar)





